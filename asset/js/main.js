$(document).ready(function() {
    $('#flipbook').turn({
        width: 3000, // Ancho del flipbook
        height: 3000, // Altura del flipbook
        autoCenter: true // Centrar automáticamente el flipbook en la página
    });
});
